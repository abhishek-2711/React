Notes from Episode 1


question -> how we can write hello world using react.
how our js code understand that .createElement .getElementById() -> this is because browser;s superpowers , js engine executes this js

1.browser do not understand what is react.
2.so first thing we will do is get react into our project.
3.get react via cdn. ( explore what is cdn ? )
4.first way to get it by cdn.
5.by adding cdn links now you can write react code , now browser will understand react syntax.
6.React can also works in your phone. so dom file is separeted from the development.js which contain the core react thing. other one is bridge between the react and dom.
7.at the end of day react is using js
8.JSX -makes our life easy when we need to create tags. we will no longer use React.createElement, but this is the core of react.
in modern react stuff 
9.React is just a library it is made using js only.
10. React can be applied to your existing project. small part of website can be made using react. 

